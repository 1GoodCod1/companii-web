# Vite env for Docker dev (docker-compose.dev.yml mounts this as /app/.env)
VITE_API_URL=/api/v1
VITE_DEV_API_PROXY_TARGET=http://host.docker.internal:4100
VITE_ENV=development
VITE_USE_HTTPONLY_COOKIE=true

# Optional: absolute media CDN
# VITE_MEDIA_URL=
